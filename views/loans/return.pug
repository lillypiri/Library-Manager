extends ../layout

block main
  h1= title
  
  include ../error
  
  form(action="/loans/" + loan.id + "?_method=PUT", method="post")
    div
      label(for="returned_on") Returned on:
      input(type="text", name="returned_on" value=d(new Date()))
    input(type="submit")